spring.application.name=demo

# Server Configuration (UNCHANGED)
server.port=9001
server.forward-headers-strategy=framework

# MySQL Database Configuration (FIXED)
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/dynamic_pricing_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration (UNCHANGED)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration (UNCHANGED)
jwt.secret=VerySecretKeyForJwtDemoApplication123456789012345678901234567890
jwt.expiration=3600000
jwt.useSecureKey=true

# Swagger/OpenAPI (UNCHANGED)
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
